# normify-pyvdisk · Normify Outline

> 派生索引（每次 normify_build 重建）。AI 导航入口：先广度后深度。

## pyvdisk（https://github.com/HeDaas-Code/pyvdisk）

- pyvdisk — PyVDisk / PyVDisk — 单机 Agentic 数据与执行基础设施：一个 .vdisk 容器承载文件系统、向量记忆、事件轨迹、检查点、WAL 与运行状态，并内置 VScript 安全工作… — [模块 272 · API 794]
  - pyvdisk.api — 公开门面 / Public Facade — 公开门面：调用方直接从 pyvdisk 包根导入的存储、执行与契约名称。 — [模块 1 · API 8]
  - pyvdisk.cli — 命令行 / Command Line — 命令行界面：参数解析以及文件系统、卷、驱动器、向量、日志与状态检查子命令。 — [模块 15 · API 59]
    - pyvdisk.cli.admin — 管理子命令 / Admin Subcommands — 管理类子命令：格式化、链接、属性、空间统计、fsck 与扩容。 — [模块 1 · API 8]
    - pyvdisk.cli.dirs — 目录子命令 / Directory Subcommands — 目录与删除子命令，语义与对应文件系统操作一致。 — [模块 1 · API 4]
    - pyvdisk.cli.drive — 驱动器子命令 / Drive Subcommands — 模拟驱动器子命令：scan、mount-all 与卷标管理。 — [模块 1 · API 3]
    - pyvdisk.cli.entry — 模块入口 / Module Entry — 让包可直接执行的 4 行模块入口。 — [模块 1 · API 1]
    - pyvdisk.cli.file-ops — 文件子命令 / File Subcommands — 整文件子命令：write、append、copy、move 与宿主导入导出。 — [模块 1 · API 6]
    - pyvdisk.cli.format — CLI 格式化 / CLI Formatting — 各子命令共用的 CLI 格式化与解析辅助。 — [模块 1 · API 4]
    - pyvdisk.cli.image — 镜像子命令 / Image Subcommands — 镜像生命周期与检查子命令：create、info、status 与列举。 — [模块 1 · API 4]
    - pyvdisk.cli.inspect — 检查子命令 / Inspection Subcommands — 检查与挂载子命令：stat、tree、mount 与 touch。 — [模块 1 · API 4]
    - pyvdisk.cli.log — 日志子命令 / Log Subcommands — 日志盘子命令：流管理、写入、查询、tail、统计、压缩与保留策略。 — [模块 1 · API 8]
    - pyvdisk.cli.main — CLI 入口 / CLI Entry Point — CLI 入口：把错误映射为退出码，以及 python -m pyvdisk 钩子。 — [模块 1 · API 1]
    - pyvdisk.cli.open — CLI 打开 VFS / CLI VFS Open — 每个文件系统子命令获取已挂载 VFS 的唯一入口。 — [模块 1 · API 1]
    - pyvdisk.cli.parser — 参数解析器 / Argument Parser — 参数解析器定义：全部子命令、各自选项与共享的镜像参数组。 — [模块 1 · API 1]
    - pyvdisk.cli.vector — 向量子命令 / Vector Subcommands — 向量盘子命令：磁盘与集合管理，以及记录 upsert、检索与获取。 — [模块 1 · API 8]
    - pyvdisk.cli.volume — 卷子命令 / Volume Subcommands — 卷子命令：创建、状态与在线镜像维护。 — [模块 1 · API 6]
  - pyvdisk.compat — 兼容垫片 / Compatibility Shims — 向后兼容垫片：保留旧的 Unified/Container 名称与遗留 vscript.checkpoint 导入路径。 — [模块 3 · API 3]
    - pyvdisk.compat.unified — Unified 别名 / Unified Aliases — 兼容重导出：保留容器化之前的 DataDisk 与 MetadataTransaction 导入路径。 — [模块 1 · API 2]
    - pyvdisk.compat.vscript — VScript 检查点垫片 / VScript Checkpoint Shim — 遗留垫片：在存储移入 infrastructure 后仍保留 vscript.checkpoint 模块路径可导入。 — [模块 1 · API 1]
  - pyvdisk.contracts — 后端契约 / Backend Contracts — 存储与执行平面的后端中立契约：能力、持久性、块/命名空间/集合/事件存储，以及执行平面协议。 — [模块 8 · API 24]
    - pyvdisk.contracts.capability — 能力授权 / Capability Grant — Capability 枚举与 CapabilityGrant：治理层、执行上下文与 VScript 共用的权限词表。 — [模块 1 · API 4]
    - pyvdisk.contracts.durability — 持久性 / Durability — 持久性请求词表：后端可以提供强于请求的保证。 — [模块 1 · API 2]
    - pyvdisk.contracts.events — 事件契约 / Event Contracts — 事件值类型，以及结构化日志使用的追加/读取事件存储协议。 — [模块 1 · API 4]
    - pyvdisk.contracts.execution — 执行上下文 / Execution Context — ExecutionContext：交给单次二层执行的依赖与策略信封。 — [模块 1 · API 3]
    - pyvdisk.contracts.plane — 执行平面协议 / Execution Plane — ExecutionPlane 协议：每个执行后端都必须暴露的唯一 submit 入口。 — [模块 1 · API 1]
    - pyvdisk.contracts.runhandle — 运行句柄 / Run Handle — RunHandle：执行平面返回的可观察、可取消句柄；取消不会覆盖终态。 — [模块 1 · API 5]
    - pyvdisk.contracts.storage — 存储协议 / Storage Protocols — 结构化存储协议：BlockDevice、NamespaceStore、CollectionStore；既有后端以结构化方式满足它们。 — [模块 1 · API 5]
  - pyvdisk.delivery — 打包与持续集成 / Packaging and CI — 分发与持续集成：打包元数据、PEP 561 类型标记与 GitHub Actions 工作流。 — [模块 4 · API 5]
    - pyvdisk.delivery.ci — 测试工作流 / Test Workflow — CI 工作流：在 push 与 pull request 上安装 dev extras 并在 3.9–3.12 矩阵上运行 pytest，使声明的 requir… — [模块 1 · API 1]
    - pyvdisk.delivery.lint — 静态分析工作流 / Lint Workflow — 独立于单元测试门禁的静态分析流水线：pylint 只保留 error 类，使 Lint 失败不会被误认为测试失败。 — [模块 1 · API 1]
    - pyvdisk.delivery.packaging — 打包元数据 / Packaging Metadata — 分发元数据：以 hnswlib 为依赖、fuse 与 dev extras、py.typed 标记，以及把 LICENSE 与 CHANGELOG.md 一并打… — [模块 1 · API 3]
  - pyvdisk.docs — 技术文档 / Technical Docs — 仓库技术文档层：架构设计、契约边界、API 治理与 VScript 语言规范，是模块结构的一手证据来源。 — [模块 7 · API 6]
    - pyvdisk.docs.api-governance — API 治理 / API Governance — API 治理：公开入口、权限模型、已废弃的旧路径，以及推荐给 Agent 使用的接口面。 — [模块 1 · API 1]
    - pyvdisk.docs.architecture — 架构设计 / Architecture Design — 架构设计：存储平面、执行平面、单盘 ACID、内部 exactly-once、解释器架构与故障恢复流程。 — [模块 1 · API 1]
    - pyvdisk.docs.changelog — 变更日志 / Changelog — 变更日志：按 issue #1 的清单编号记录用户可见变更，并列出四条明确留待后续的项目。 — [模块 1 · API 1]
    - pyvdisk.docs.contracts — 契约边界 / Contract Boundaries — 各平面的保证边界：存储、执行、事务与内部 exactly-once 承诺什么、不承诺什么；新增 VScript 事务的撤销记账、本地触发器与审计轨迹三节。 — [模块 1 · API 1]
    - pyvdisk.docs.readme — 项目总览 / Project Overview — README 总览：项目定位、快速开始、核心能力、CLI 用法与验证方式；徽章与声明的 Python 下限及当前用例数一致。 — [模块 1 · API 1]
    - pyvdisk.docs.vscript-spec — VScript 规范 / VScript Spec — VScript 规范：语法、标准库、解释器流水线、能力模型、错误体系与 CLI 入口。 — [模块 1 · API 1]
  - pyvdisk.execution — 执行平面 / Execution Plane — 执行平面：同步 ExecutionService、持久任务队列、持久化运行状态与操作注册表。 — [模块 22 · API 72]
    - pyvdisk.execution.operations — 操作注册表 / Operation Registry — 操作注册表：稳定 operation id、负载指纹、持久化结果、可调用对象解析与 VScript 操作描述。 — [模块 7 · API 24]
      - pyvdisk.execution.operations.callables — 可调用对象注册表 / Callable Registry — 可调用对象注册表，以及把 Python 函数变为可持久重放操作的 RegisteredCallable 包装。 — [模块 1 · API 4]
      - pyvdisk.execution.operations.describe — 操作描述 / Operation Description — 操作描述：把可调用对象、VScript 源码与裸负载归一为可序列化形态并生成稳定 id。 — [模块 1 · API 4]
      - pyvdisk.execution.operations.errors — 操作错误 / Operation Errors — 操作注册表错误体系：未知操作与不可序列化操作。 — [模块 1 · API 3]
      - pyvdisk.execution.operations.record — 操作记录 / Operation Record — 存储的操作描述及其 JSON 编解码，是稳定 operation id 与幂等性的基础。 — [模块 1 · API 3]
      - pyvdisk.execution.operations.registry — 操作注册表 / Operation Registry — 操作注册表门面：存在判定、加载、结果恢复、保存与删除，背衬可为检查点或宿主目录。 — [模块 1 · API 7]
      - pyvdisk.execution.operations.storage — 注册表存储 / Registry Storage — 背衬访问与可调用对象解析，注册表的最底层。 — [模块 1 · API 3]
    - pyvdisk.execution.queue — 持久任务队列 / Durable Queue — DurableQueue：以检查点为背衬的任务队列，支持 claim、lease、心跳、退避重试、死信、取消与过期回收。 — [模块 6 · API 23]
      - pyvdisk.execution.queue.errors — 队列错误 / Queue Errors — 队列错误体系：任务缺失、非法状态迁移与租约丢失。 — [模块 1 · API 4]
      - pyvdisk.execution.queue.lifecycle — 任务生命周期 / Task Lifecycle — 任务生命周期：入队、带租约领取、心跳、完成、失败、重试、死信与取消。 — [模块 1 · API 8]
      - pyvdisk.execution.queue.recovery — 队列恢复 / Queue Recovery — 可观测性与崩溃恢复：过期租约回收，以及任务查询与列举。 — [模块 1 · API 3]
      - pyvdisk.execution.queue.store — 队列存储 / Queue Store — 队列存储层：经检查点存储的状态读写，以及重试退避策略。 — [模块 1 · API 6]
      - pyvdisk.execution.queue.task — 队列任务 / Queue Task — 队列状态中存储的任务值对象，含租约与尝试次数记账。 — [模块 1 · API 2]
    - pyvdisk.execution.runstate — 运行状态 / Run State Store — RunStateStore：带版本号的运行记录，受保护的状态转换与过期运行回收，经检查点存储持久化。 — [模块 3 · API 10]
      - pyvdisk.execution.runstate.model — 运行记录 / Run Record — 运行记录模型，以及受保护状态迁移使用的乐观并发冲突类型。 — [模块 1 · API 3]
      - pyvdisk.execution.runstate.store — 运行状态存储 / Run State Store — 运行状态存储：带幂等的创建、受保护状态迁移、列举与过期运行回收。 — [模块 1 · API 7]
    - pyvdisk.execution.service — 执行服务 / Execution Service — ExecutionService：以 inline 或可选 worker 方式执行 Python 可调用对象与 VScript 源码，绑定 DataDisk、队… — [模块 5 · API 15]
      - pyvdisk.execution.service.invoke — 操作分发 / Operation Dispatch — 操作分发：受限视图构造与两种执行类型，即 Python 可调用对象与 VScript 源码。 — [模块 1 · API 4]
      - pyvdisk.execution.service.queue — 队列驱动执行 / Queue-Driven Run — 队列驱动的执行：带去重的操作入队，以及带租约处理的领取-运行循环。 — [模块 1 · API 3]
      - pyvdisk.execution.service.setup — 服务初始化 / Service Setup — ExecutionService 构造与 worker 池控制：同步内联执行与后台处理之间的边界。 — [模块 1 · API 5]
      - pyvdisk.execution.service.submit — 提交与取消 / Submit and Cancel — ExecutionPlane 协议的实现：submit、阻塞式 run 与取消。 — [模块 1 · API 3]
  - pyvdisk.governance — 能力治理 / Capability Governance — 能力治理：路径归一化、受保护命名空间适配器，以及交给不可信调用方的 ScopedDataDisk。 — [模块 5 · API 26]
    - pyvdisk.governance.data — 受限数据服务 / Scoped Data Services — 能力治理的向量与日志半边：按集合与流划分范围，区分读、写、追加与 admin 权限。 — [模块 1 · API 8]
    - pyvdisk.governance.files — 受限文件系统 / Scoped Filesystem — 能力治理的文件系统半边：每个 VFS 方法都必须经过权限与范围检查才能触达。 — [模块 1 · API 8]
    - pyvdisk.governance.namespace — 能力门禁 / Capability Gate — 能力门禁：路径归一、范围包含判定，以及所有受限命名空间共用的受保护委托辅助。 — [模块 1 · API 5]
    - pyvdisk.governance.scoped — 受限容器 / Scoped Container — 检查点范围限定，以及执行层交给不可信代码的 ScopedDataDisk 对象与工厂。 — [模块 1 · API 5]
  - pyvdisk.storage — 存储平面 / Storage Plane — 存储平面：块镜像、磁盘身份、盘上文件系统、高层 VFS、多盘卷、向量盘与日志盘、结构化日志、模拟驱动器、可选 FUSE 挂载、统一 DataDisk 容器、WA… — [模块 108 · API 377]
    - pyvdisk.storage.block — 块镜像 / Block Image — 块级镜像 IO：把一个 .vdisk 文件当作块设备，使用 os.pread/os.pwrite 与 POSIX advisory 写锁。 — [模块 4 · API 12]
      - pyvdisk.storage.block.access — 块访问 / Block Access — 已打开镜像上的块级与字节级访问：块读写、裸字节区间与镜像扩缩。 — [模块 1 · API 5]
      - pyvdisk.storage.block.image — 镜像生命周期 / Image Lifecycle — VirtualDisk 生命周期：稀疏创建、加锁打开、上下文管理、刷新与关闭。 — [模块 1 · API 5]
      - pyvdisk.storage.block.io — 定位 IO 原语 / Positional IO — 无缓冲定位读写原语，供镜像上的所有块级与字节级访问使用。 — [模块 1 · API 2]
    - pyvdisk.storage.checkpoint — 检查点存储 / Checkpoint Store — 检查点存储：以 VFS 路径或宿主目录为背衬的持久键值进度与结果记录，带跨进程锁与原子落盘。 — [模块 4 · API 11]
      - pyvdisk.storage.checkpoint.io — 检查点 IO / Checkpoint IO — 检查点 IO：键值记录映射的加载、读取、暂存与原子保存。 — [模块 1 · API 4]
      - pyvdisk.storage.checkpoint.open — 检查点存储 / Checkpoint Store — 检查点存储的构造与加锁：后端选择、VFS 或宿主背衬，以及独占写周期。 — [模块 1 · API 5]
      - pyvdisk.storage.checkpoint.triggers — 触发器检查点 / Trigger Checkpoints — 面向触发器的便利层，为调度器提供持久的事件去重保证。 — [模块 1 · API 2]
    - pyvdisk.storage.datadisk — DataDisk 容器 / DataDisk Container — 统一 DataDisk 容器：一个挂载的 VFS 加上 FS/向量/日志命名空间服务，带撤销日志的元数据事务、WAL 与重挂载恢复。 — [模块 8 · API 39]
      - pyvdisk.storage.datadisk.api — DataDisk 接口 / DataDisk Interface — 容器生命周期与面向调用方的接口：挂载、manifest、事务、元数据访问与能力受限视图。 — [模块 1 · API 8]
      - pyvdisk.storage.datadisk.codec — 元数据编解码 / Metadata Codec — DataDisk 的值编码与错误词表，供事务、manifest 与命名空间层共用。 — [模块 1 · API 5]
      - pyvdisk.storage.datadisk.core — DataDisk 核心 / DataDisk Core — DataDisk 构造、元数据原子持久化，以及挂载时的 WAL 重放与恢复报告。 — [模块 1 · API 6]
      - pyvdisk.storage.datadisk.namespace — 文件系统命名空间 / Filesystem Namespace — 事务性文件系统命名空间：读方法透传给 VFS，而每次变更都登记撤销意图。 — [模块 1 · API 8]
      - pyvdisk.storage.datadisk.services — 服务命名空间 / Service Namespaces — 向量与日志命名空间：透传到各自磁盘，同时作为一等事务参与者。 — [模块 1 · API 2]
      - pyvdisk.storage.datadisk.tx — 元数据事务 / Metadata Transaction — 元数据事务核心：参与者协议、暂存写入，以及使命名空间变更可回滚的撤销日志。 — [模块 1 · API 6]
      - pyvdisk.storage.datadisk.tx-lifecycle — 事务结局 / Transaction Outcome — 事务的完整结局：进入、经 WAL 的持久提交、带撤销的回滚，以及异常驱动的自动回滚。abort 现在先把 intent 落盘，再按逆序在进程内通知每个已登记的 … — [模块 1 · API 4]
    - pyvdisk.storage.driver — 模拟驱动器 / Drive Manager — 模拟驱动器：扫描宿主目录、按尾标识别盘、组装卷并按卷标挂载。 — [模块 6 · API 27]
      - pyvdisk.storage.driver.assembled — 卷组装 / Volume Assembly — 单个卷的组装结果：找到哪些成员以及必须按何顺序打开。 — [模块 1 · API 5]
      - pyvdisk.storage.driver.lifecycle — 卸载与热插拔 / Unmount and Hotplug — 模拟驱动器的卸载、热插拔重扫与诊断汇总。 — [模块 1 · API 4]
      - pyvdisk.storage.driver.manager — 磁盘管理器 / Disk Manager — 驱动器扫描与组装：识别宿主目录中的介质及其成员如何聚合成卷。 — [模块 1 · API 5]
      - pyvdisk.storage.driver.mounting — 挂载分发 / Mount Dispatch — 挂载分发：每类介质一条路径，以及 CLI 使用的整体挂载与卷标查询。 — [模块 1 · API 7]
      - pyvdisk.storage.driver.mounts — 挂载介质 / Mounted Media — 日志盘、向量盘与文件系统介质的统一挂载包装，使驱动器可统一处理。 — [模块 1 · API 6]
    - pyvdisk.storage.fs — 文件系统 / Filesystem — 盘上文件系统：超级块、inode 表、位图、间接块指针、目录、路径解析、fsck 与在线扩容。 — [模块 24 · API 82]
      - pyvdisk.storage.fs.attrs — 属性与链接 / Attributes and Links — 属性与链接：硬链接、模式、属主、时间戳，以及 VFS 使用的权限检查。 — [模块 1 · API 5]
      - pyvdisk.storage.fs.bitmap — 位图分配 / Bitmap Allocation — inode 与数据块分配共用的位图原语。 — [模块 1 · API 3]
      - pyvdisk.storage.fs.block-alloc — 块分配器 / Block Allocator — 建立在块位图之上的数据块分配器。 — [模块 1 · API 2]
      - pyvdisk.storage.fs.create — 创建节点 / Node Creation — 节点创建：文件、目录、符号链接及其共享构造器。 — [模块 1 · API 5]
      - pyvdisk.storage.fs.dir — 目录读取 / Directory Read — 目录读取路径：目录项解码、列举与按名查找。 — [模块 1 · API 3]
      - pyvdisk.storage.fs.dir-mutate — 目录写入 / Directory Write — 目录写入路径：槽位查找、目录项插入与删除。 — [模块 1 · API 3]
      - pyvdisk.storage.fs.fsck — 一致性检查 / Filesystem Check — 一致性检查器：校验超级块、交叉核对 inode 与块位图、链接数与目录项，并支持修复模式。 — [模块 1 · API 2]
      - pyvdisk.storage.fs.grow — 在线扩容 / Online Grow — 在线扩容：扩大底层镜像并重写超级块几何。 — [模块 1 · API 1]
      - pyvdisk.storage.fs.inode — Inode / Inode — inode 结构及其二进制编解码与空节点工厂。 — [模块 1 · API 4]
      - pyvdisk.storage.fs.inode-io — Inode 表 IO / Inode Table IO — inode 表 IO：128 字节 inode 记录的分配、释放与定位读写。 — [模块 1 · API 5]
      - pyvdisk.storage.fs.layout — 盘上布局 / On-Disk Layout — 盘上布局常量：魔数、版本、inode 与目录项几何，以及推导出的文件尺寸上限。 — [模块 1 · API 5]
      - pyvdisk.storage.fs.locking — FS 加锁 / FS Locking — 逐方法读写加锁，以及让同一镜像上的多个 FS 实例互斥的跨实例锁注册表。 — [模块 1 · API 4]
      - pyvdisk.storage.fs.mkfs — 格式化 / Format — 格式化：构建带保留区的空文件系统，并写入磁盘身份尾标；身份写入不可用时不影响格式化。 — [模块 1 · API 1]
      - pyvdisk.storage.fs.mount — 挂载 / Mount — FS 构造、挂载时超级块校验与超级块持久化。 — [模块 1 · API 3]
      - pyvdisk.storage.fs.mutate — 删除与重命名 / Delete and Rename — 删除与重命名：unlink、rmdir、节点解绑与目标替换。 — [模块 1 · API 4]
      - pyvdisk.storage.fs.names — 名字编解码 / Name Codec — 目录槽位名编解码，以及跨文件系统接口使用的 FSError。 — [模块 1 · API 4]
      - pyvdisk.storage.fs.paths — 路径解析 / Path Resolution — 路径解析：分量拆分、带深度上限的符号链接感知查找，以及创建所需的父目录解析。 — [模块 1 · API 3]
      - pyvdisk.storage.fs.pointers — 块指针 / Block Pointers — 直接与间接块指针树：解析、扩展与回收 inode 的块映射。 — [模块 1 · API 5]
      - pyvdisk.storage.fs.query — 状态与空间查询 / Stat and Space Queries — 只读查询：stat 变体、空闲对象计数，以及 df/du 使用的 statfs 汇总。 — [模块 1 · API 6]
      - pyvdisk.storage.fs.readwrite — 文件读写 / File Read and Write — 文件数据路径：定位读取、追加、覆盖与按需分配块的偏移写入。 — [模块 1 · API 4]
      - pyvdisk.storage.fs.stat — Stat 视图 / Stat View — stat 结果对象及类型判定，供 VFS、FUSE 与 CLI 使用。 — [模块 1 · API 4]
      - pyvdisk.storage.fs.superblock — 超级块 / Superblock — 超级块结构及其二进制编解码；镜像上所有区域偏移的唯一事实来源。 — [模块 1 · API 3]
      - pyvdisk.storage.fs.truncate — 截断 / Truncate — 截断操作及其后续的间接块清理。 — [模块 1 · API 3]
    - pyvdisk.storage.fuse — FUSE 挂载 / FUSE Mount — 可选 FUSE 挂载：把 VFS 暴露为宿主目录；缺少 fusepy 时给出清晰的 ImportError。 — [模块 5 · API 20]
      - pyvdisk.storage.fuse.data — FUSE 数据处理 / FUSE Data Ops — FUSE 数据与命名空间变更：read、write、create、mkdir、rmdir、unlink 与 rename 处理。 — [模块 1 · API 7]
      - pyvdisk.storage.fuse.handlers — FUSE 链接处理 / FUSE Link Ops — 其余 FUSE 处理：truncate、链接、open/release、chmod，以及动态 Operations 子类装配。readlink 以不追随后一段的… — [模块 1 · API 7]
      - pyvdisk.storage.fuse.meta — FUSE 属性 / FUSE Attributes — FUSE 可用性检测，以及操作表中属性与目录列举部分。 — [模块 1 · API 4]
      - pyvdisk.storage.fuse.mount — FUSE 挂载入口 / FUSE Mount Entry — 公开挂载入口，包含容忍忙挂载的 main 循环补丁与线程化变体。 — [模块 1 · API 2]
    - pyvdisk.storage.identity — 磁盘身份 / Disk Identity — 磁盘身份：block 0 末尾的 112 字节 VDID 尾标，让每块 .vdisk 不依赖路径即可被识别。 — [模块 5 · API 12]
      - pyvdisk.storage.identity.format — 身份格式 / Identity Format — 尾标布局常量、.vdisk 后缀规则与身份错误类型。 — [模块 1 · API 3]
      - pyvdisk.storage.identity.model — 身份模型 / Identity Model — DiskIdentity 值对象及其类型判定与简短展示形式。 — [模块 1 · API 4]
      - pyvdisk.storage.identity.probe — 身份探测 / Identity Probe — 身份 IO：写尾标、不挂载即探测盘文件，以及 UUID 生成。 — [模块 1 · API 3]
      - pyvdisk.storage.identity.trailer — 尾标编解码 / Trailer Codec — 身份尾标的二进制编解码：定长 112 字节布局的打包与解析。 — [模块 1 · API 2]
    - pyvdisk.storage.log — 日志盘 / Log Disk — 日志盘：分段事件流，带序号、游标、ack、replay、保留策略与压缩。 — [模块 7 · API 22]
      - pyvdisk.storage.log.append — 日志追加 / Log Append — 事件摄入：单条与批量追加，自动分段。 — [模块 1 · API 2]
      - pyvdisk.storage.log.filter — 日志过滤 / Log Filtering — 日志记录值过滤与规范 JSON 编码，供追加与查询共用。 — [模块 1 · API 3]
      - pyvdisk.storage.log.open — 日志盘打开 / Log Disk Open — LogDisk 生命周期及日志命名空间下按流的 JSON 辅助。 — [模块 1 · API 4]
      - pyvdisk.storage.log.query — 日志查询 / Log Query — 日志盘读取侧：带过滤查询、消费端游标、ack、replay、计数、tail 与实时 follow。 — [模块 1 · API 7]
      - pyvdisk.storage.log.retention — 保留与压缩 / Retention and Compaction — 日志盘的存储治理：保留策略执行、压缩与统计。保留如今同时清理被删事件的 event_ids 并校验消费者游标，不再留下指向已裁剪位置的游标。 — [模块 1 · API 3]
      - pyvdisk.storage.log.streams — 日志流 / Log Streams — 流目录：带保留策略的创建、列举与删除。 — [模块 1 · API 3]
    - pyvdisk.storage.logging — 结构化日志 / Structured Logging — 结构化日志核心：级别归一、LogEvent、上下文绑定、可插拔 sink、UnifiedLogger 与标准库 logging 桥接。 — [模块 6 · API 18]
      - pyvdisk.storage.logging.bridge — 日志桥接 / Logging Bridge — 与标准库 logging 模块的桥接，以及模块级日志器工厂。 — [模块 1 · API 2]
      - pyvdisk.storage.logging.context — 日志上下文 / Log Context — 基于 contextvar 的环境字段，会合入每个产出的事件。 — [模块 1 · API 3]
      - pyvdisk.storage.logging.events — 日志事件 / Log Events — 结构化事件值类型及其级别归一与 JSON 转换。 — [模块 1 · API 4]
      - pyvdisk.storage.logging.logger — 统一日志器 / Unified Logger — UnifiedLogger：级别门限、上下文与字段合并、标签绑定与异常捕获。 — [模块 1 · API 5]
      - pyvdisk.storage.logging.sinks — 日志 Sink / Log Sinks — sink 协议及其三种实现：内存、流与日志盘持久化。 — [模块 1 · API 4]
    - pyvdisk.storage.rwlock — 读写锁 / Read-Write Lock — 可重入、写者优先的读写锁，供同一镜像上并发打开的多个 FS 实例共享。 — [模块 3 · API 9]
      - pyvdisk.storage.rwlock.core — 锁核心 / Lock Core — 锁核心：每线程重入、写者优先，以及仅限唯一读者时的读锁升级。 — [模块 1 · API 5]
      - pyvdisk.storage.rwlock.ctx — 锁上下文 / Lock Contexts — 供 FS 锁装饰器与 VFS/FS 公开接口使用的 with 语句辅助类型。 — [模块 1 · API 4]
    - pyvdisk.storage.vector — 向量盘 / Vector Disk — 向量盘：基于 HNSW 的具名记录集合，带 generation、checksum、过滤器与相似度检索。 — [模块 5 · API 16]
      - pyvdisk.storage.vector.collections — 集合目录 / Collections — 集合目录：按维度与度量创建、带配置列举，以及连同每集合 HNSW 索引一起删除。 — [模块 1 · API 3]
      - pyvdisk.storage.vector.open — 向量盘打开 / Vector Disk Open — VectorDisk 生命周期，以及后续操作共用的 JSON 辅助与集合路径键化。 — [模块 1 · API 5]
      - pyvdisk.storage.vector.records — 向量记录 / Vector Records — 记录 CRUD：维度校验、单条与批量 upsert、获取、删除与计数。 — [模块 1 · API 5]
      - pyvdisk.storage.vector.search — 向量检索 / Vector Search — HNSW 图上的索引管理与相似度检索，支持元数据过滤。 — [模块 1 · API 3]
    - pyvdisk.storage.vfs — 虚拟文件系统 / Virtual File System — FS 之上的高层路径式 VFS：类文件对象、权限检查、宿主导入导出、遍历、空间统计与卷自动识别。 — [模块 17 · API 62]
      - pyvdisk.storage.vfs.attrs — 元数据变更 / Metadata Writes — VFS 门面上的元数据变更：模式、属主、时间戳、权限检查与截断。 — [模块 1 · API 5]
      - pyvdisk.storage.vfs.create — 创建盘与卷 / Create Disk or Volume — VFS 门面上的磁盘与卷创建入口。 — [模块 1 · API 2]
      - pyvdisk.storage.vfs.dirs — 目录创建 / Directory Creation — 目录创建辅助，含用于命名空间初始化的递归变体。 — [模块 1 · API 2]
      - pyvdisk.storage.vfs.files — 整文件 IO / Whole-File IO — 建立在定位式 FS 文件 API 之上的整文件读、写与追加便利接口。 — [模块 1 · API 3]
      - pyvdisk.storage.vfs.hostio — 宿主交换 / Host Interchange — 宿主交换：文件对象打开，以及虚拟与宿主文件系统之间的单文件与递归导入导出。 — [模块 1 · API 4]
      - pyvdisk.storage.vfs.lifetime — 挂载生命周期 / Mount Lifecycle — 挂载与关闭生命周期，包含测试与 CLI 广泛使用的 with 协议。 — [模块 1 · API 4]
      - pyvdisk.storage.vfs.open — VFS 打开 / VFS Open — VFS 构造与传输选择：单个镜像可按普通盘或多成员卷打开。 — [模块 1 · API 3]
      - pyvdisk.storage.vfs.permissions — 权限门禁 / Permission Gate — 每次 VFS 变更前的权限门禁：inode 模式位、路径遍历与父目录检查。 — [模块 1 · API 5]
      - pyvdisk.storage.vfs.query — VFS 查询 / VFS Queries — VFS 门面上的存在性、类型判定与目录列举。 — [模块 1 · API 5]
      - pyvdisk.storage.vfs.remove — 删除与重命名 / Delete and Rename — 删除与重命名操作，含检查点清理使用的递归目录删除。 — [模块 1 · API 4]
      - pyvdisk.storage.vfs.space — 空间与维护 / Space and Maintenance — VFS 门面暴露的诊断与维护能力：空间统计、一致性检查与在线扩容。 — [模块 1 · API 5]
      - pyvdisk.storage.vfs.stat — 元数据读取 / Metadata Reads — 暴露给调用方与 FUSE 的元数据读取与链接操作。 — [模块 1 · API 5]
      - pyvdisk.storage.vfs.vfile — VFile 对象 / VFile Object — 打开文件对象的 VFile 构造与 inode 自省。 — [模块 1 · API 3]
      - pyvdisk.storage.vfs.vfile-ctx — VFile 协议 / VFile Protocol — 打开 VFile 对象的 with 语句与迭代协议。 — [模块 1 · API 3]
      - pyvdisk.storage.vfs.vfile-io — VFile 读写 / VFile IO — 打开 VFile 上的定位读写、seek、tell、truncate、flush 与 close。 — [模块 1 · API 8]
      - pyvdisk.storage.vfs.walk — 目录遍历 / Directory Walk — 递归目录遍历生成器，供 import_host_tree、du 与 CLI 树视图使用。 — [模块 1 · API 1]
    - pyvdisk.storage.volume — 多盘卷 / Multi-Disk Volume — 多盘卷：concat、stripe、mirror 三种模式对外沿用单盘的块 IO 接口，成员元数据存于 block 0。 — [模块 9 · API 31]
      - pyvdisk.storage.volume.blocks — 逻辑块 / Logical Blocks — 按模式分发到具体地址映射的逻辑块入口。 — [模块 1 · API 3]
      - pyvdisk.storage.volume.bytes — 字节传输 / Byte Transport — 字节级传输、描述符复制与 CLI 使用的状态报告。 — [模块 1 · API 4]
      - pyvdisk.storage.volume.create — 卷创建 / Volume Creation — VFS 门面与 CLI 使用的卷创建与打开辅助。 — [模块 1 · API 2]
      - pyvdisk.storage.volume.layout — RAID 映射 / RAID Mapping — 分模式地址映射：拼接、条带与镜像，镜像读取支持副本回退。 — [模块 1 · API 7]
      - pyvdisk.storage.volume.meta — 卷元数据 / Volume Metadata — 卷元数据模型：写入每个成员的描述符及其内部的成员记录。 — [模块 1 · API 5]
      - pyvdisk.storage.volume.model — 卷模型 / Volume Model — 卷模型，以及按模式把成员容量换算为逻辑地址空间的几何计算。 — [模块 1 · API 3]
      - pyvdisk.storage.volume.open — 卷打开 / Volume Open — 卷打开路径：成员打开、跨成员一致性校验与关闭/刷新生命周期。 — [模块 1 · API 4]
      - pyvdisk.storage.volume.repair — 镜像维护 / Mirror Maintenance — 在线镜像维护：新增、移除与重新同步副本。 — [模块 1 · API 3]
    - pyvdisk.storage.wal — 预写日志 / Write-Ahead Log — 预写日志：同一套带检查点与截断的分段 WAL，由 DataDisk 事务与 VScript 事务撤销共用，而不是两份并行实现。 — [模块 4 · API 16]
      - pyvdisk.storage.wal.entries — WAL 记录 / WAL Entries — 事务日志动词与崩溃恢复重放：begin/append/prepare/commit/abort，以及重放所依赖的读取入口。 — [模块 1 · API 8]
      - pyvdisk.storage.wal.journal — 元数据 WAL / Metadata WAL — DataDisk 事务使用的元数据预写日志：存储后端、行编解码，以及分配序号的追加写入。 — [模块 1 · API 4]
      - pyvdisk.storage.wal.undo — 宿主撤销 WAL / Host Undo WAL — VScript 侧的 WAL 入口：独立的宿主路径实现已取消——本模块直接复用同一套 WriteAheadLog，并给出撤销记录使用的 kind 集与编解码。 — [模块 1 · API 4]
  - pyvdisk.tests — 测试套件 / Test Suite — 测试套件：391 项可执行校验，覆盖文件系统、卷、身份、锁、队列、事务、VScript、审计轨迹、FUSE 桥与打包。 — [模块 56 · API 71]
    - pyvdisk.tests.agentic-cli — 智能体 CLI 用例 / Agentic CLI Tests — 面向智能体使用编写的 CLI 用例：可发现的输出与稳定的退出码。 — [模块 1 · API 1]
    - pyvdisk.tests.api-governance — API 治理用例 / API Governance Tests — 检查公开能力 API 是否拒绝应当拒绝的调用，而不只验证正常路径。 — [模块 1 · API 1]
    - pyvdisk.tests.atomic-save — 原子保存用例 / Atomic Save Tests — 验证元数据写入是全有或全无，即事务层所依赖的性质。 — [模块 1 · API 1]
    - pyvdisk.tests.audit-trail — 审计轨迹用例 / Audit Trail Tests — 审计轨迹用例：哈希链校验（含在真实日志段内篡改记录）、查询筛选与保留策略。 — [模块 1 · API 1]
    - pyvdisk.tests.cancellation — 取消用例 / Cancellation Tests — 验证协作式取消会让队列与运行状态保持一致，不停留在中间状态。 — [模块 1 · API 1]
    - pyvdisk.tests.checkpoint — 检查点用例 / Checkpoint Tests — 为队列、运行、操作与审计提供背衬的持久键值存储的单元用例。 — [模块 1 · API 1]
    - pyvdisk.tests.contracts — 契约用例 / Contract Tests — 保持抽象契约与具体实现一致的合规性用例。 — [模块 1 · API 1]
    - pyvdisk.tests.crash-matrix — 崩溃矩阵用例 / Crash Matrix Tests — 最强的持久性用例：在每个阶段中断事务并断言恢复后落在合法状态。 — [模块 1 · API 1]
    - pyvdisk.tests.e2e — 端到端用例 / End-to-End Tests — 遍历整个基础设施栈的集成用例，含在受限盘上运行脚本。 — [模块 1 · API 1]
    - pyvdisk.tests.exactly-once-internal — 内部精确一次用例 / Exactly-Once Internal Tests — 验证重放操作不会重复生效，即精确一次的核心承诺。 — [模块 1 · API 1]
    - pyvdisk.tests.exactly-once-operation — 操作精确一次用例 / Exactly-Once Operation Tests — 精确一次契约在操作层的半边，经公开执行接口观察。 — [模块 1 · API 1]
    - pyvdisk.tests.execution — 执行用例 / Execution Tests — 执行平面用例：分发、受限视图与结果传递。 — [模块 1 · API 1]
    - pyvdisk.tests.execution-queue — 执行队列用例 / Execution Queue Tests — 把执行服务绑定到持久队列及其租约语义的集成用例。 — [模块 1 · API 1]
    - pyvdisk.tests.fixtures — 测试夹具 / Test Fixtures — 全套件各测试模块共享的 pytest 夹具定义。 — [模块 1 · API 1]
    - pyvdisk.tests.fuse-mount — FUSE 挂载用例 / FUSE Mount Tests — FUSE 桥用例：属性与目录操作、truncate/link/chmod，以及此前对任何符号链接都返回 EINVAL 的 lstat 语义 readlink。 — [模块 1 · API 1]
    - pyvdisk.tests.governance — 治理回归用例 / Governance Regression Tests — 最大的治理测试模块：治理工作新增的受限命名空间的回归覆盖。 — [模块 1 · API 1]
    - pyvdisk.tests.logdisk-compact — 日志压缩用例 / Log Compaction Tests — 守住压缩绝不可违反的性质：减少存储不得改变可观测的流状态。 — [模块 1 · API 1]
    - pyvdisk.tests.logdisk-retention — 日志保留用例 / Log Retention Tests — 验证保留策略不会删除消费者尚未确认的事件。 — [模块 1 · API 1]
    - pyvdisk.tests.packaging — 打包用例 / Packaging Tests — 验证包可构建安装，且 py.typed 标记与控制台入口完好。 — [模块 1 · API 1]
    - pyvdisk.tests.participant-compensation — 参与者补偿用例 / Participant Compensation Tests — 参与者补偿用例：持久化写前补偿、进程内 abort 的逆序分发、跨挂载的幂等重复补偿。 — [模块 1 · API 1]
    - pyvdisk.tests.queue — 队列用例 / Queue Tests — 单进程内持久队列的状态迁移用例。 — [模块 1 · API 1]
    - pyvdisk.tests.queue-backoff — 退避与锁用例 / Backoff and Lock Tests — 对指数退避与串行化队列变更的锁的时序敏感用例。 — [模块 1 · API 1]
    - pyvdisk.tests.queue-crossprocess — 跨进程队列用例 / Cross-Process Queue Tests — 证明持久队列的租约与去重保证跨进程边界成立，而非仅限线程。 — [模块 1 · API 1]
    - pyvdisk.tests.submit-conflict — 提交冲突用例 / Submit Conflict Tests — 交互用例：提交时的版本冲突不得留下写一半的 VFS 变更。 — [模块 1 · API 1]
    - pyvdisk.tests.tx-idempotency — 事务幂等用例 / Transaction Idempotency Tests — 小而关键的用例：同一事务提交两次不会产生副作用。 — [模块 1 · API 1]
    - pyvdisk.tests.tx-rollback — 事务回滚用例 / Transaction Rollback Tests — 撤销日志用例：失败事务内的文件系统变更必须被精确还原。 — [模块 1 · API 1]
    - pyvdisk.tests.vdisk — VFS 行为规格 / VFS Behaviour Spec — 以真实镜像上的测试类表达的 VFS/FS/卷/锁/CLI/向量/日志行为规格。 — [模块 20 · API 34]
      - pyvdisk.tests.vdisk.base — VFS 测试基类 / VFS Test Base — 测试基类与基础路径用例：启动镜像的夹具，以及最小的 VFS 行为。 — [模块 1 · API 2]
      - pyvdisk.tests.vdisk.cli — CLI 用例 / CLI Tests — CLI 契约用例：以用户方式调用每个子命令并断言其输出形态。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.disk-manager — 磁盘管理用例 / Disk Manager Tests — 驱动层用例：挂载记账与已挂载文件系统的管理接口。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.exhaustion — 空间耗尽用例 / Space Exhaustion Tests — 空间耗尽行为：分配失败必须上报，不得静默损坏。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.identity — 身份用例 / Identity Tests — 身份用例：让宿主文件可被识别为 pyvdisk 镜像的尾标。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.io — 持久性与 IO 用例 / Persistence and IO Tests — 持久性与 IO 边界用例：重挂载持久性、宿主传输与非法路径错误。 — [模块 1 · API 3]
      - pyvdisk.tests.vdisk.large-files — 大文件与删除用例 / Large File and Remove Tests — 间接块与目录删除行为，即块记账与 inode 复用最易出错的两处。 — [模块 1 · API 2]
      - pyvdisk.tests.vdisk.links — 链接与属性用例 / Link and Attribute Tests — inode 层行为：硬链接、属性修改与稀疏分配。 — [模块 1 · API 3]
      - pyvdisk.tests.vdisk.locking — 并发用例 / Concurrency Tests — 并发用例：单独验证锁原语，以及真实并发访问下的 VFS。 — [模块 1 · API 2]
      - pyvdisk.tests.vdisk.log — 日志盘用例 / Log Disk Tests — 日志盘与日志集成用例，即存储平面用例的末端。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.maintenance — 维护用例 / Maintenance Tests — 维护类用例：一致性检查、就地扩容与空间统计。 — [模块 1 · API 3]
      - pyvdisk.tests.vdisk.names — 命名空间用例 / Namespace Tests — 命名空间语义：重命名、符号链接与文件对象接口。 — [模块 1 · API 3]
      - pyvdisk.tests.vdisk.permissions — 权限用例 / Permission Tests — 权限用例：跨 uid 与 gid 组合遵守读、写、执行位。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.raw-fs — 裸文件系统用例 / Raw Filesystem Tests — 直接操作块设备与磁盘布局、绕过 VFS 门面的用例。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.vector — 向量盘用例 / Vector Disk Tests — 向量盘行为，即存储平面同时承载的非文件系统负载。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.volume — 卷模式用例 / Volume Mode Tests — 拼接与条带两种无冗余模式的卷用例。 — [模块 1 · API 3]
      - pyvdisk.tests.vdisk.volume-detect — 卷检测用例 / Volume Detection Tests — 从设备内容而非配置自动检测卷成员关系。 — [模块 1 · API 1]
      - pyvdisk.tests.vdisk.volume-meta — 卷元数据用例 / Volume Metadata Tests — 卷元数据与错误处理，以及卷 CLI 接口。 — [模块 1 · API 3]
      - pyvdisk.tests.vdisk.volume-mirror — 镜像卷用例 / Mirror Volume Tests — 镜像模式用例：区别于拼接与条带的冗余契约。 — [模块 1 · API 1]
    - pyvdisk.tests.vscript — VScript 用例 / VScript Tests — VScript 语言用例加上针对真实盘的端到端运行，是全套件中最广的单个测试模块。 — [模块 1 · API 3]
    - pyvdisk.tests.vscript-audit — 审计用例 / Audit Tests — 对一次运行后审计记录必须包含的内容，以及两种 sink 均持久化它的断言。 — [模块 1 · API 1]
    - pyvdisk.tests.vscript-cron — Cron 用例 / Cron Tests — Cron 用例：字段解析、日/星期并集语义、步长与区间写法，以及下次触发时间计算。 — [模块 1 · API 1]
    - pyvdisk.tests.vscript-host — 宿主隔离用例 / Host Confinement Tests — 验证 host 模块拒绝根外路径——VScript 所依赖的安全边界。 — [模块 1 · API 1]
    - pyvdisk.tests.vscript-optional — 可选链用例 / Optional Chain Tests — 语言边界用例：可选成员访问如何短路而非报错。 — [模块 1 · API 1]
    - pyvdisk.tests.vscript-triggers — 触发器用例 / Trigger Tests — 触发器用例：单调水位线排序（此前用随机事件 id 作文本比较会静默丢事件）、注册时空 pattern/stream 的拒绝，以及 poll_once 行为。 — [模块 1 · API 1]
    - pyvdisk.tests.vscript-undo — 撤销用例 / Undo Tests — 撤销用例：每个 fs 动词在生效前记账、大正文落盘、restore_tree 与崩溃后遗留快照的清理。 — [模块 1 · API 1]
    - pyvdisk.tests.wal-checkpoint — WAL 检查点用例 / WAL Checkpoint Tests — WAL 检查点用例：检查点后的截断、不再全量重放的恢复，以及 DataDisk 与 VScript 共用的同一套实现。 — [模块 1 · API 1]
    - pyvdisk.tests.wal-recovery — WAL 恢复用例 / WAL Recovery Tests — 日志重放的专项用例：已回滚事务在恢复后必须不可见。 — [模块 1 · API 1]
  - pyvdisk.vscript — VScript 语言 / VScript Language — VScript：安全工作流语言，含词法、语法、树遍历运行时、能力受限标准库、策略/预算、审计与调度。 — [模块 42 · API 143]
    - pyvdisk.vscript.audit — VScript 审计 / VScript Audit — VScript 审计：稳定的脚本与策略哈希、带执行平面关联字段的 AuditRecord，以及持久化 sink。 — [模块 6 · API 25]
      - pyvdisk.vscript.audit.clock — 运行标识 / Run Identity — 用于填充审计记录的运行标识与耗时测量。 — [模块 1 · API 2]
      - pyvdisk.vscript.audit.hashing — 审计哈希 / Audit Hashing — 脚本与策略的确定性哈希，以及审计哈希链：每行都带上前驱哈希，verify 报告第一条不匹配的记录——包括在真实日志段内做的手脚。 — [模块 1 · API 6]
      - pyvdisk.vscript.audit.query — 审计查询与保留 / Audit Query and Retention — 审计轨迹的读取侧：按运行/任务/状态/时间筛选记录，并支持显式保留裁剪，使轨迹既可查也可有界，而不是只能追加。 — [模块 1 · API 2]
      - pyvdisk.vscript.audit.record — 审计记录 / Audit Record — 解释器、执行服务与 Sink 共用的审计记录形态，含使其可校验（而非仅可追加）的链字段。 — [模块 1 · API 2]
      - pyvdisk.vscript.audit.sinks — 审计 Sink / Audit Sinks — 持久审计目的地：检查点存储里的有界保留，或日志盘上的只追加审计流。两者现在都可读：查询筛选、显式保留、哈希链校验，以及供外部锚定的链头。 — [模块 1 · API 13]
    - pyvdisk.vscript.cli — VScript 命令行 / VScript CLI — VScript 命令行入口：check、run、run-disk 与交互式 REPL，含挂载授权参数解析。 — [模块 4 · API 8]
      - pyvdisk.vscript.cli.commands — VScript 命令 / VScript Commands — VScript 命令接口：参数解析、挂载授权与 check/run 入口，含使 host.* 可用的宿主读/写根目录选项。 — [模块 1 · API 3]
      - pyvdisk.vscript.cli.parser — VScript 参数注册 / VScript Parser Hook — 把 VScript 接口挂到 pyvdisk 主 CLI 上的 argparse 注册。 — [模块 1 · API 1]
      - pyvdisk.vscript.cli.repl — VScript REPL / VScript REPL — 交互式 VScript REPL：行包装、值格式化、帮助，以及跨输入保持状态的循环。 — [模块 1 · API 4]
    - pyvdisk.vscript.cron — Cron 表达式 / Cron Expressions — 定时触发器所需的 cron 支持：字段解析、匹配与下次触发计算。 — [模块 1 · API 5]
    - pyvdisk.vscript.frontend — VScript 前端 / VScript Front End — VScript 前端：词法单元、最大匹配 lexer、AST 节点、递归下降语句解析器、Pratt 表达式解析器与带位置诊断。 — [模块 7 · API 30]
      - pyvdisk.vscript.frontend.ast — AST 节点 / AST Nodes — AST 节点词表：表达式、语句、声明，以及工作流特有的 transaction、task、parallel 与 await 节点。 — [模块 1 · API 8]
      - pyvdisk.vscript.frontend.errors — 错误类型 / Error Types — 错误层次：把语言各阶段映射为可区分、可捕获、可审计的失败。 — [模块 1 · API 8]
      - pyvdisk.vscript.frontend.lexer — 词法器 / Lexer — 词法器：注释与空白处理、数字与字符串字面量、运算符与最大匹配。 — [模块 1 · API 5]
      - pyvdisk.vscript.frontend.parser — 语法解析器 / Parser — 语法实现：语句、声明、控制流、match 分支与工作流构造。 — [模块 1 · API 2]
      - pyvdisk.vscript.frontend.span — 位置与诊断 / Span and Diagnostic — 位置与诊断原语：事情发生在哪里，以及如何向用户报告。 — [模块 1 · API 4]
      - pyvdisk.vscript.frontend.tokens — 词法单元 / Token Types — 语言的词法单元词表：lexer 能产出的全部关键字、运算符与字面量。 — [模块 1 · API 3]
    - pyvdisk.vscript.host — 宿主访问 / Host Access — VScript 宿主访问：白名单 HostProxy，把脚本文件传输限制在声明的目录根内。 — [模块 3 · API 7]
      - pyvdisk.vscript.host.guard — 宿主隔离 / Host Confinement — 宿主隔离：声明脚本可读写的宿主目录，其余路径一律拒绝。该策略现在有真实入口——CLI 注入声明的根目录——host.* 不再因根目录为空而不可用。 — [模块 1 · API 6]
      - pyvdisk.vscript.host.module — 宿主模块 / Host Module — host 原生模块：面向脚本的读取、写入与传输入口，全部经隔离代理。 — [模块 1 · API 1]
    - pyvdisk.vscript.mounts — 挂载注册表 / Mount Registry — 挂载授权：把 CLI 挂载参数转为能力受限句柄并在运行后关闭的注册表。 — [模块 1 · API 6]
    - pyvdisk.vscript.policy — 策略与预算 / Policy and Budget — VScript 策略与预算：调用深度、循环、IO 与 Host 传输限额，以及脚本可 require 的受保护能力集。 — [模块 3 · API 8]
      - pyvdisk.vscript.policy.budget — 策略与预算 / Policy and Budget — 脚本的资源治理：声明式限额与执行计费并强制限额的运行时计数器。 — [模块 1 · API 6]
      - pyvdisk.vscript.policy.capability — 脚本能力 / Script Capability — 脚本可见的能力对象及其权限门禁。 — [模块 1 · API 2]
    - pyvdisk.vscript.runtime — VScript 运行时 / VScript Runtime — VScript 运行时：树遍历求值器、词法 Env 链、闭包、控制流、确定性 task/parallel、事务与模块加载。 — [模块 8 · API 29]
      - pyvdisk.vscript.runtime.callables — 可调用对象与任务 / Callables and Tasks — 可调用对象与任务：参数绑定、对定义作用域的闭包，以及 await 等待的任务句柄。 — [模块 1 · API 5]
      - pyvdisk.vscript.runtime.core — 解释器核心 / Interpreter Core — 解释器核心：构造与原生模块安装、带参数的顶层运行、原生模块调用的记账入口，以及语句块执行。 — [模块 1 · API 6]
      - pyvdisk.vscript.runtime.env — 作用域与控制流 / Scope and Flow — 作用域与控制流机制：Env 链，以及以异常实现 return、break、continue 的信号。 — [模块 1 · API 7]
      - pyvdisk.vscript.runtime.expressions — 表达式求值 / Expression Evaluation — 表达式求值：字面量、运算符、调用、成员与下标访问，以及赋值语义。 — [模块 1 · API 4]
      - pyvdisk.vscript.runtime.modules — 模块加载 / Module Loading — 模块加载：在内置原生模块与已配置模块根下解析 import 名称。 — [模块 1 · API 1]
      - pyvdisk.vscript.runtime.statements — 语句执行 / Statement Execution — 语句执行：解释器中最大的分发表，覆盖语言的全部语句种类。 — [模块 1 · API 1]
      - pyvdisk.vscript.runtime.tx — 脚本事务 / Script Transaction — 脚本事务支持：每个可撤销的 fs 变更在生效前落账，进程内回滚与崩溃恢复走同一个撤销解释器，超过 64 KiB 的快照正文落到事务目录。 — [模块 1 · API 5]
    - pyvdisk.vscript.stdlib — VScript 标准库 / VScript Standard Library — VScript 标准库：fs、vector、log、json 的能力受限原生模块，脚本只能通过 Handle 包装访问。 — [模块 4 · API 9]
      - pyvdisk.vscript.stdlib.factory — 标准库工厂 / Stdlib Factory — 标准库工厂：文件系统、向量、日志与 json 原生函数，均受能力守卫与字节预算约束。每个会改动的 fs 动词在触碰存储之前先记入撤销账，事务失败时可回滚，而不留… — [模块 1 · API 1]
      - pyvdisk.vscript.stdlib.guards — 原生守卫 / Native Guards — 每个原生函数触及资源前调用的两个守卫：类型与权限检查，以及路径范围限定。 — [模块 1 · API 2]
      - pyvdisk.vscript.stdlib.native — 原生模块 / Native Modules — 原生模块基础设施与 Handle 包装：让原生资源留在能力检查之后，而非暴露裸 Python 对象。 — [模块 1 · API 6]
    - pyvdisk.vscript.triggers — 触发器 / Trigger Registry — VScript 调度：持久触发器注册表，带事件去重与轮询守护进程执行注册脚本。 — [模块 3 · API 7]
      - pyvdisk.vscript.triggers.daemon — 调度守护进程 / Scheduler Daemon — 轮询守护进程：每轮评估到期的文件与日志触发器并分发动作，按单调水位线排序事件，不再静默跳过到期事件。 — [模块 1 · API 3]
      - pyvdisk.vscript.triggers.registry — 触发器注册表 / Trigger Registry — 触发器的注册与列举。注册即校验输入（空文件 pattern 以前能注册成功、随后把轮询循环打挂），水位线比较改用单调事件序号，而不再拿随机 uuid 作文本比较… — [模块 1 · API 4]
    - pyvdisk.vscript.undo — 事务撤销记账 / Transaction Undo Bookkeeping — VScript 事务的撤销记账：每个可撤销的 fs 变更在生效之前就落账，进程内回滚与崩溃恢复共用同一个解释器；超过 64 KiB 的快照正文落到 /.syst… — [模块 1 · API 9]

